-- Vehicle Booking System Database Setup
-- Run this script to create the database

CREATE DATABASE IF NOT EXISTS vehicle_booking_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE vehicle_booking_db;

-- Create a user for the application (optional)
-- CREATE USER 'vbs_user'@'localhost' IDENTIFIED BY 'secure_password';
-- GRANT ALL PRIVILEGES ON vehicle_booking_db.* TO 'vbs_user'@'localhost';
-- FLUSH PRIVILEGES;

-- Note: Tables will be created automatically by Sequelize when you run npm run seed



